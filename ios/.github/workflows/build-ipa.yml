name: Build IPA

on:
  push:
    branches: [main, master]
    paths:
      - "ios/**"
      - ".github/workflows/build-ipa.yml"
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode (use latest 16.x)
        run: |
          # macos-15 默认带 Xcode 16.x，挑选实际存在的最新 Xcode
          LATEST=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          echo "Selecting $LATEST"
          sudo xcode-select -s "$LATEST"

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        working-directory: ios
        run: xcodegen generate

      - name: List schemes (debug)
        working-directory: ios
        run: xcodebuild -list

      - name: Verify Info.plist settings (debug)
        working-directory: ios
        run: |
          echo "=== Build Settings ==="
          xcodebuild -project WorkBuddyRemote.xcodeproj -scheme WorkBuddyRemote -configuration Release -showBuildSettings 2>/dev/null | grep -E "INFOPLIST_FILE|GENERATE_INFOPLIST_FILE|MARKETING_VERSION|CURRENT_PROJECT_VERSION"
          echo "=== Source Info.plist ==="
          cat Sources/WorkBuddyRemote/Info.plist

      - name: Build Release app (unsigned)
        working-directory: ios
        run: |
          xcodebuild \
            -project WorkBuddyRemote.xcodeproj \
            -scheme WorkBuddyRemote \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            -derivedDataPath build \
            build

      - name: Package IPA
        working-directory: ios
        run: |
          APP_PATH=$(find build/Build/Products/Release-iphoneos -name "WorkBuddyRemote.app" -type d | head -1)
          echo "Found app at: $APP_PATH"
          echo "=== Built Info.plist ==="
          plutil -p "$APP_PATH/Info.plist" || echo "plutil not available, trying cat"
          echo "=== LaunchScreen resources ==="
          find "$APP_PATH" -name "*Launch*" -o -name "*.storyboardc" | head -10
          echo "=== All app bundle files ==="
          find "$APP_PATH" -type f | sort
          rm -rf Payload
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -qr WorkBuddyRemote.ipa Payload
          ls -lh WorkBuddyRemote.ipa
          unzip -l WorkBuddyRemote.ipa | head -20

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: WorkBuddyRemote-unsigned-ipa
          path: ios/WorkBuddyRemote.ipa
          retention-days: 30
          if-no-files-found: error
